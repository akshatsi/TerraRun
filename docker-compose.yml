

services:
  db:
    image: postgis/postgis:15-3.4
    container_name: terrarun_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-terrarun}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-terrarun_secret}
      POSTGRES_DB: ${POSTGRES_DB:-terrarun}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U terrarun"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: terrarun_backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  pgdata:
